package com.reimbursement.backend.enums;

public enum BillStatus {
    DRAFT,
    SUBMITTED,
    APPROVED_BY_MANAGER,
    REJECTED_BY_MANAGER,
    APPROVED_BY_FINANCE,
    REJECTED_BY_FINANCE,
    CLOSED
}
